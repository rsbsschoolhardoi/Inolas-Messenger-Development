rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Global Safety Net (default allow for seamless app usage and preview)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
